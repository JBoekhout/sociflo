#runs all non_local_checks
#verified operationscheme in abstract
#

data_labels = {
    'loop_times': 0
}
class node_optive():
    def __init__(self, ):
        loop_t = 
        
    def node_poll():
        DB_COMMS
        stase_up_dates = read_node(self, d=loop_times)
    def main():
        xstack = {  0: None,
                    1: None,
                    2: None,
                    3: None,
                    4: None} # delist for optimized array s; operate a higher lvl opsequence for ultra/deadpriority lists.
        for each in xstack:
            if each.