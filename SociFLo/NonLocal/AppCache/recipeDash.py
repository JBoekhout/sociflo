#for each in __name__.join(manifest):
ID=099
try:
    import Encyclopack.thrD.framework as 3dframeset # enyclopack is the local enyclopedia
except ModuleNotFoundError:
    #networkcomms for dependencie's install, chain verifies
def update(): #modularize in software engine
    import drivers.nodeintfcing.SoftwareToolkit as nodecall 
    datuple = nodecall.update(ID)
    if datuple[0]:
        uf = open("updatecp".join(ID),mode='rw')
        #Update overaches modulecontainer or app-container in hw running for updated stacks logically. 
    else:
    #check logs, updates if neccary , other messages


def dlcipe:
    pass

def 