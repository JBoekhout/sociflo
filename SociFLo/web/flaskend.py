import os #serverhostundec.
from flask import Flask, request, render_template
import RouteSide.nodingFunctionals as 

app = Flask(__name__)
app.conf['DEBUG_ENV'] = 1

STDcolor = "rgba(13, 71, 228, 0.842)"
Colorsets= {'mission-ctr': [STDcolor, "rgb(67, 166, 239)", 'rgb(19,0,143)'],
            'earth': ["#7fba00", "#00a4ef", '#FFE082'],
            'StrawBCream': ['#EF9A9A','#FFD180']}
            ""
wheels = {"td": }
labelstack = {"Activit types":["PreProductives", "Bootsequence", "ENergetics", "Ecstatics", "Construction", "Creation", "Socials", "Extrals", "details", "personaltaskstack"],
                "Personelia":["Hardware","Inventoire","Skillsetview","Credits","History","Smilecreds"]
             # {idstring: }
inertstatE = #active constructs realit  bases

import sqlalchemy/

def calc_theme(data):
    '''
        define theme settings according to available data on best to go to or default for excitement to random osmth
    '''

class ANIMSTACK:
    def refer(label):
        #WEB_LIB INNARD REFSET #could do with a ref to(being based p2p) encyclemboss destructing lazy labels to hardsets. mafait.
        pass

    stack = {"SarveCross": [refer("Globe"), refer("Sol"), refer("SolSys"),]}
def verifyUID(uid, data): #~rehashing a uid every login so next log is prebuilt - single key auth # no bruteforce passwork
    gra


@app.route("/")
def fp():
    with open("networkstate").read() as o:
        if o[0] == 1: # inert alert I dont rememer situal logy.
            alert=True
        else:
            pass
    #set theme
    uidprop = request.cookies.get("uid")

    if verifyID(uidprop)
    
@app.route("/REDISTRIb")
def localinventorycacheIF():
    if not auth(request.cookies.get('key')):
        return render_template('unauth.html')
    localeID = 
    

@app.route("/auth", methods=["POST", "GET"])
def auth():
    if request.method == 'POST':
        user, lock = request.form['usr'], request.form['key'] #q-hooks for alltypes access auth, mainly the ID carrier bound by body and sit'state

    page=0
    user = request.
    
    
            


