APPID= -100
class shell:
    def __init__(self):
        pass
    def run(self):
        pass
    def runIMG(self, sw: swtraceIMG, )

class swtraceIMG: #defining hw io for perched computing.
    def pollHash(dt: Sieve(APPID+'h')) -> str: #dt containing scoped function hashes for validity check
        for label, dr in dt:
            globals()[label] = dr
            
        with open('./drivers/coldstor/'+)
    def rscdelimit():
        #Delimit HW operations, contingencies & return perched operation space; Builds a report of execute range of superceding softwarePack
